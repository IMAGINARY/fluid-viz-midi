import { AnimatableSimulationParameters } from './parameter-animator';

const INITIAL = {
  DENSITY_DISSIPATION: 2,
  VELOCITY_DISSIPATION: 1,
  PRESSURE: 0.8,
  CURL: 4,
  SPLAT_RADIUS: 0.15,
};

const FAST_SWIRLS = {
  DENSITY_DISSIPATION: 1.7,
  VELOCITY_DISSIPATION: 0,
  PRESSURE: 0.9544213,
  CURL: 7,
  SPLAT_RADIUS: 0.16,
};

const CENTERED_ROUND_FAST = {
  DENSITY_DISSIPATION: 0,
  VELOCITY_DISSIPATION: 4,
  PRESSURE: 0,
  CURL: 0,
  SPLAT_RADIUS: 0.216,
};

const SMALL_PUSHY_SWIRLS = {
  DENSITY_DISSIPATION: 0,
  VELOCITY_DISSIPATION: 0,
  PRESSURE: 0,
  CURL: 0,
  SPLAT_RADIUS: 0.4,
};
const ROUND_FULL = {
  DENSITY_DISSIPATION: 0.125,
  VELOCITY_DISSIPATION: 4,
  PRESSURE: 1,
  CURL: 0,
  SPLAT_RADIUS: 0.06,
};

const ROUND_THIN = {
  DENSITY_DISSIPATION: 0.125,
  VELOCITY_DISSIPATION: 4,
  PRESSURE: 0,
  CURL: 0,
  SPLAT_RADIUS: 0.06,
};

const VERY_CURLY_CENTER = {
  DENSITY_DISSIPATION: 4,
  VELOCITY_DISSIPATION: 0,
  PRESSURE: 0,
  CURL: 50,
  SPLAT_RADIUS: 0.216,
};

const SMEARY_FAST_NICE = {
  CURL: 1,
  DENSITY_DISSIPATION: 1,
  PRESSURE: 0.0625,
  SPLAT_RADIUS: 0.188,
  VELOCITY_DISSIPATION: 4,
};

const SMEARY_FULL = {
  CURL: 1,
  DENSITY_DISSIPATION: 0.1,
  PRESSURE: 0.6,
  SPLAT_RADIUS: 0.173,
  VELOCITY_DISSIPATION: 4,
};
const SQUID_SWIRLS = {
  CURL: 35,
  DENSITY_DISSIPATION: 1.431,
  PRESSURE: 1,
  SPLAT_RADIUS: 0.283,
  VELOCITY_DISSIPATION: 4,
};

const parameterSets: AnimatableSimulationParameters[] = [
  INITIAL,
  FAST_SWIRLS,
  CENTERED_ROUND_FAST,
  SMALL_PUSHY_SWIRLS,
  ROUND_FULL,
  ROUND_THIN,
  VERY_CURLY_CENTER,
  SMEARY_FAST_NICE,
  SMEARY_FULL,
  SQUID_SWIRLS,
];

export default parameterSets;
